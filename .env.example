# .env.example
# This file serves as a template for the required environment variables.
# Copy this file to .env.dev for local development and fill in the values.
# DO NOT COMMIT SENSITIVE DATA like SECRET_KEY or API keys to version control.

# -- Django Core Settings --
# Generate a new secret key for your local environment.
# You can generate one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1

# -- External API Keys --
# Get your API key from https://www.geoapify.com/
GEOAPIFY_API_KEY=

# -- PostgreSQL Database Settings --
# These credentials are used by Docker Compose to initialize the database container.
# You can leave these as they are for local development.
POSTGRES_DB=eld_trip_planner_db
POSTGRES_USER=eld_trip_planner_user
POSTGRES_PASSWORD=eld_trip_planner_password

# -- Django's Connection to the Database --
# These variables are used by Django's settings.py to connect to the database.
# The SQL_HOST must match the database service name in docker-compose.yml (e.g., "db").
SQL_ENGINE=django.db.backends.postgresql
SQL_DATABASE=eld_trip_planner_db
SQL_USER=eld_trip_planner_user
SQL_PASSWORD=eld_trip_planner_password
SQL_HOST=db
SQL_PORT=5432